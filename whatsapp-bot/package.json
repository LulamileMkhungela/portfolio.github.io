{
  "name": "lulasync-whatsapp-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "LulaSync WhatsApp Cloud API auto-reply bot (Cloudflare Workers)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
